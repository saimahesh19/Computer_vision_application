<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#E6E6FA"> <!-- Lavender background -->

    <ImageView
        android:id="@+id/numberPlateImageView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        android:layout_marginBottom="16dp"
        android:background="#ADD8E6"/> <!-- Light Blue Background for Image View -->

    <TextView
        android:id="@+id/resultTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Detected text will appear here"
        android:textSize="18sp"
        android:textColor="#00008B"
    android:layout_below="@id/numberPlateImageView"
    android:layout_marginTop="16dp"
    android:gravity="center" />

    <Button
        android:id="@+id/processButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Process Image"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/resultTextView"
        android:background="#00008B"
    android:textColor="#FFFFFF"
    android:layout_marginTop="20dp" />
</RelativeLayout>
